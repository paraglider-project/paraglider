server: 
  host: "localhost"
  port: 8080
  rpcPort: 8081

cloudPlugins:
  - name: "gcp"
    host: "localhost"
    port: 1001

namespaces: 
  default:
    cloudDeployments:
      - name: "gcp"
        deployment: "projects/project-name"
      - name: "azure"
        deployment: "/subscriptions/sub-id/resourceGroups/rg-name"

tagService:
  host: "localhost"
  port: 6000
